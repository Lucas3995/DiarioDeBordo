---
phase: 03-acervo-b-sico
task: verify-work
total_tasks: 6-plans-executed
status: in_progress
last_updated: 2026-04-04T08:21:59.580Z
---

<current_state>
Fase 3 (Acervo Básico) foi totalmente executada (6 plans, todos com SUMMARY.md). Estamos na etapa de validação manual (/gsd:verify-work 3). Dois bugs foram corrigidos nesta sessão. O último fix (Bug 2 — botão Vincular desabilitado) foi aplicado mas NÃO confirmado pelo usuário. Há 3 arquivos modificados não commitados com esse fix.

Fix mais recente (não testado pelo usuário):
- ViewModel: nova prop `TipoSelecionadoItem` (object?) — setter chama `SelecionarOuCriarTipoRelacao`
- AXAML: `SelectedItem="{Binding TipoSelecionadoItem, Mode=TwoWay}"` no TipoRelacaoAutoComplete
- Code-behind: AsyncPopulator + KeyDown only (removido SelectionChanged — não dispara confiável no Avalonia 11)

Raiz do bug: `SelectionChanged` no Avalonia 11 AutoCompleteBox não dispara confiravelmente. Solução: binding TwoWay em `SelectedItem` como sinal de seleção.
</current_state>

<completed_work>

- Plan 01-06: Todos executados (6 SUMMARYs existem)
- Bug 1 (Visualizar crasha): `vm.CarregarCommand.Execute(null)` → `await vm.CarregarCommand.ExecuteAsync(null)` — DbContext concorrente corrigido. CONFIRMADO OK pelo usuário.
- Bug 2 (Vincular desabilitado): TipoSelecionadoItem + SelectedItem TwoWay binding. APLICADO, aguarda confirmação.
- Item 1 (Criar conteúdo): validado OK
- Item 2 (Editar conteúdo): validado OK
- Item 3 (Avaliação): validado OK
- Item 4 (Categorias): validado OK
- Excluir conteúdo: validado OK
</completed_work>

<remaining_work>

- [ ] Confirmar Bug 2 fix (botão Vincular) — usuário precisa testar relações
- [ ] Item 5 (Relações): autocomplete tipo mostra existentes; selecionar habilita botão; vincular bufferiza "(não salvo)"; Salvar persiste; relação inversa aparece no outro conteúdo
- [ ] Item 6 (Sessões): registrar sessão (início/fim/progresso/nota); timeline aparece; múltiplas sessões
- [ ] Item 7 (Paginação): com >20 itens, botões Anterior/Próxima funcionam
- [ ] Rodar `/gsd:verify-work 3` (verificação formal)
- [ ] Rodar `/gsd:ship 3` (PR + merge para main)
</remaining_work>

<decisions_made>

- `AsyncPopulator` ao invés de `Populating` event para AutoCompleteBox no Avalonia 11 (API correta)
- `window.Show()` sem owner ao invés de `Show(parent)` — owned windows ficam always-on-top no X11
- Relações bufferizadas localmente (IsPendente=true), persistidas apenas no Salvar
- `SelectedItem` TwoWay binding como sinal de seleção confiável (SelectionChanged não dispara)
- `await vm.CarregarCommand.ExecuteAsync(null)` antes de `PreCarregarTiposRelacaoAsync` — evita DbContext concorrente
- Usuário temporário hardcoded: `Guid.Parse("00000000-0000-0000-0000-000000000001")` (multi-user é Fase 8)
</decisions_made>

<blockers>

- Bug 2 fix aguarda confirmação do usuário. Se ainda não funcionar: investigar se `SelectedItem` binding no Avalonia 11 AutoCompleteBox requer que o item esteja na lista via `AsyncPopulator` para acionar o setter.
</blockers>

<context>
Arquivos modificados não commitados (Bug 2 fix):
- src/DiarioDeBordo.UI/ViewModels/ConteudoDetalheViewModel.cs — TipoSelecionadoItem prop + reset em CancelarFormularioRelacao
- src/DiarioDeBordo.UI/Views/ConteudoDetalheWindow.axaml — SelectedItem="{Binding TipoSelecionadoItem, Mode=TwoWay}" FilterMode="None"
- src/DiarioDeBordo.UI/Views/ConteudoDetalheWindow.axaml.cs — AsyncPopulator + KeyDown (sem SelectionChanged)

`PodeVincular` formula: `ConteudoAlvoSelecionado is not null && (TipoRelacaoSelecionado is not null || !string.IsNullOrWhiteSpace(NomeInversoNovoTipo))`

`SelecionarOuCriarTipoRelacao(nome)` seta `TipoRelacaoSelecionado` e aciona `PodeVincular`.

134 testes passando, 0 falhas, 0 warnings de build.

Próxima fase após validação: Fase 4 — Adaptadores RSS e YouTube.
</context>

<next_action>
1. Pedir ao usuário para testar o botão Vincular conteúdos (Item 5)
2. Se funcionar: commit "fix: SelectedItem binding para seleção de tipo de relação", depois testar Item 6 (Sessões) e Item 7 (Paginação)
3. Se não funcionar: investigar ConteudoDetalheViewModel.cs — checar se SelecionarOuCriarTipoRelacao notifica PodeVincular corretamente (NotifyCanExecuteChanged)
</next_action>
