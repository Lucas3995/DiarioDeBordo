---
phase: 03-acervo-b-sico
plan: 04
type: execute
wave: 4
depends_on: ["03-03"]
files_modified:
  - src/DiarioDeBordo.UI/Services/IDialogService.cs
  - src/DiarioDeBordo.UI/Services/DialogService.cs
  - src/DiarioDeBordo.UI/Views/ConfirmacaoDialog.axaml
  - src/DiarioDeBordo.UI/Views/ConfirmacaoDialog.axaml.cs
  - src/DiarioDeBordo.UI/Views/ConteudoDetalheWindow.axaml
  - src/DiarioDeBordo.UI/Views/ConteudoDetalheWindow.axaml.cs
  - src/DiarioDeBordo.UI/ViewModels/ConteudoDetalheViewModel.cs
  - src/DiarioDeBordo.UI/ViewModels/ConfirmacaoDialogViewModel.cs
  - src/DiarioDeBordo.UI/Views/AcervoView.axaml
  - src/DiarioDeBordo.UI/ViewModels/AcervoViewModel.cs
  - src/DiarioDeBordo.Desktop/App.axaml.cs
autonomous: true
requirements:
  - ACE-01
  - ACE-02
  - ACE-09

must_haves:
  truths:
    - "Clicking 'Ver detalhe' icon button on card opens ConteudoDetalheWindow as modal"
    - "Modal shows 4 collapsible sections (Identificação expanded, others collapsed)"
    - "Modal has fixed footer with Excluir (left), Cancelar + Salvar (right)"
    - "Closing modal with unsaved changes shows discard confirmation dialog (D-02)"
    - "Clicking Excluir (card or modal) shows delete confirmation dialog (D-03)"
    - "Card shows metadata row with category count, classificação icon, nota"
    - "Card title uses 14px body size (normalized from 15px)"
  artifacts:
    - path: "src/DiarioDeBordo.UI/Views/ConteudoDetalheWindow.axaml"
      provides: "Detail/edit modal window"
      contains: "Expander"
    - path: "src/DiarioDeBordo.UI/ViewModels/ConteudoDetalheViewModel.cs"
      provides: "ViewModel with dirty tracking"
      contains: "IsDirty"
    - path: "src/DiarioDeBordo.UI/Views/ConfirmacaoDialog.axaml"
      provides: "Reusable confirmation dialog"
      contains: "SizeToContent"
    - path: "src/DiarioDeBordo.UI/Services/IDialogService.cs"
      provides: "Dialog abstraction for MVVM"
      contains: "ShowDialogAsync"
  key_links:
    - from: "src/DiarioDeBordo.UI/ViewModels/AcervoViewModel.cs"
      to: "src/DiarioDeBordo.UI/Services/IDialogService.cs"
      via: "Opens modal via service"
      pattern: "_dialogService"
    - from: "src/DiarioDeBordo.UI/ViewModels/ConteudoDetalheViewModel.cs"
      to: "src/Modules/Module.Acervo/Commands/AtualizarConteudoCommand.cs"
      via: "MediatR Send"
      pattern: "AtualizarConteudoCommand"
---

<objective>
Build the ConteudoDetalheWindow modal, confirmation dialog, and enhanced content card with action buttons.

Purpose: This is the primary editing interface — users open the modal via the card's "Ver detalhe" button, see 4 collapsible Expander sections, edit content, and save/cancel/delete with proper confirmation dialogs and dirty state tracking.
Output: ConteudoDetalheWindow (AXAML + ViewModel), ConfirmacaoDialog (reusable), IDialogService, enhanced AcervoView card with metadata row and action buttons.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-acervo-b-sico/03-CONTEXT.md
@.planning/phases/03-acervo-b-sico/03-UI-SPEC.md
@.planning/phases/03-acervo-b-sico/03-RESEARCH.md
@.planning/phases/03-acervo-b-sico/03-03-SUMMARY.md

<interfaces>
<!-- Key types from Plans 01-03 the UI consumes -->

From src/Modules/Module.Acervo/DTOs/ConteudoDetalheDto.cs (enriched in Plan 03):
```csharp
public sealed record ConteudoDetalheDto(
    Guid Id, string Titulo, string? Descricao, string? Anotacoes,
    decimal? Nota, string Formato, string Papel, DateTimeOffset CriadoEm,
    Classificacao? Classificacao, EstadoProgresso EstadoProgresso,
    string? PosicaoAtual, string? Subtipo, bool IsFilho,
    int? TotalEsperadoSessoes,
    IReadOnlyList<CategoriaDto> Categorias,
    IReadOnlyList<RelacaoDto> Relacoes,
    IReadOnlyList<SessaoDto> Sessoes,
    int SessoesContagem);
```

From src/Modules/Module.Acervo/DTOs/ConteudoResumoDto.cs (enriched in Plan 03):
```csharp
public sealed record ConteudoResumoDto(
    Guid Id, string Titulo, string Formato, string Papel,
    DateTimeOffset CriadoEm, Classificacao? Classificacao,
    int CategoriaCount, decimal? Nota);
```

Commands consumed by ViewModel:
- ObterConteudoQuery(Guid Id, Guid UsuarioId) → Resultado<ConteudoDetalheDto>
- AtualizarConteudoCommand(...) → Resultado<Unit>
- ExcluirConteudoCommand(Guid Id, Guid UsuarioId) → Resultado<Unit>
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dialog services + ConteudoDetalheWindow + ConfirmacaoDialog</name>
  <files>
    src/DiarioDeBordo.UI/Services/IDialogService.cs
    src/DiarioDeBordo.UI/Services/DialogService.cs
    src/DiarioDeBordo.UI/Views/ConfirmacaoDialog.axaml
    src/DiarioDeBordo.UI/Views/ConfirmacaoDialog.axaml.cs
    src/DiarioDeBordo.UI/ViewModels/ConfirmacaoDialogViewModel.cs
    src/DiarioDeBordo.UI/Views/ConteudoDetalheWindow.axaml
    src/DiarioDeBordo.UI/Views/ConteudoDetalheWindow.axaml.cs
    src/DiarioDeBordo.UI/ViewModels/ConteudoDetalheViewModel.cs
    src/DiarioDeBordo.Desktop/App.axaml.cs
  </files>
  <action>
    **1. Create `IDialogService.cs`** in `src/DiarioDeBordo.UI/Services/`:
    ```csharp
    public interface IDialogService
    {
        Task<bool?> MostrarConteudoDetalheAsync(Guid conteudoId);
        Task<bool> MostrarConfirmacaoAsync(string titulo, string mensagem, string botaoPrimario, string botaoSecundario, bool isPrimarioDestructivo = false);
    }
    ```
    `MostrarConteudoDetalheAsync` returns `true` if content was modified (needs list refresh), `false` if cancelled, `null` if deleted.

    **2. Create `DialogService.cs`** — `internal sealed class` implementing `IDialogService`:
    - Inject: main `Window` reference (obtained via DI — pass `Avalonia.Application.Current.ApplicationLifetime` cast to `IClassicDesktopStyleApplicationLifetime` → `.MainWindow`)
    - `MostrarConteudoDetalheAsync`: create `ConteudoDetalheWindow`, set `DataContext` to a new `ConteudoDetalheViewModel` (resolved from DI), call `window.ShowDialog<bool?>(_mainWindow)`
    - `MostrarConfirmacaoAsync`: create `ConfirmacaoDialog` with `ConfirmacaoDialogViewModel(titulo, mensagem, primario, secundario, isDestructivo)`, call `ShowDialog<bool>(_mainWindow)`

    **3. Create `ConfirmacaoDialog.axaml`** (per UI-SPEC section 9):
    - `Window` with `SizeToContent="WidthAndHeight"`, `WindowStartupLocation="CenterOwner"`, `ShowInTaskbar="False"`, `CanResize="False"`
    - MinWidth="360", MaxWidth="480"
    - Padding="32,24" (per UI-SPEC spacing: 2xl horizontal, xl vertical)
    - Content: Title (20px SemiBold), Message (14px body, TextWrapping), StackPanel with two buttons (Secondary left, Primary right)
    - Primary button: if destructive → `Background="{DynamicResource SystemControlErrorTextForegroundBrush}"` or fallback destructive color; if not → accent
    - Both buttons close dialog with result (true/false) via `Close(result)` in code-behind
    - `AutomationProperties.Name="Dialog de confirmação: {titulo}"`

    **4. Create `ConfirmacaoDialog.axaml.cs`**:
    - Handle primary button click → `Close(true)`
    - Handle secondary button click → `Close(false)`

    **5. Create `ConfirmacaoDialogViewModel.cs`**:
    ```csharp
    public sealed class ConfirmacaoDialogViewModel
    {
        public string Titulo { get; }
        public string Mensagem { get; }
        public string BotaoPrimario { get; }
        public string BotaoSecundario { get; }
        public bool IsPrimarioDestructivo { get; }
    }
    ```

    **6. Create `ConteudoDetalheWindow.axaml`** (per UI-SPEC section 1 + research Pattern 8):
    - `Window` with `Width="720"`, `Height="640"`, `MinWidth="600"`, `MinHeight="500"`, `WindowStartupLocation="CenterOwner"`, `ShowInTaskbar="False"`, `CanResize="True"`
    - `Title` bound to `"{Binding TituloJanela}"` (shows "Detalhe do conteúdo" + " •" when dirty)
    - `Grid RowDefinitions="*,Auto"`:
      - Row 0: `ScrollViewer` with `StackPanel Spacing="16"` containing 4 `Expander` controls:
        1. **Identificação** (`IsExpanded="True"`): Título TextBox (auto-focused), Descrição TextBox (multiline, AcceptsReturn, Height=80), Anotações TextBox (multiline, Height=80), Formato ComboBox (ItemsSource=FormatoMidia values), Subtipo TextBox
        2. **Avaliação** (`IsExpanded="False"`): placeholder content — "Nota e classificação serão implementados no Plan 05". Leave empty StackPanel with comment, will be wired in Plan 05.
        3. **Organização** (`IsExpanded="False"`): placeholder — "Categorias e relações serão implementados no Plan 05". Leave empty StackPanel.
        4. **Histórico de Consumo** (`IsExpanded="False"`, `IsVisible="{Binding !IsFilho}"`): placeholder — "Timeline e sessões serão implementados no Plan 06". Leave empty StackPanel.
      - Each Expander has `AutomationProperties.Name="Seção {nome}: {resumo}"`
      - Expander headers show summary text when collapsed (bind to computed properties: e.g., "Nota: 8.5 · 👍" for Avaliação, "3 categorias · 2 relações" for Organização, "12 de 24 (50%)" for Histórico)
      - Row 1: Fixed footer `Border` with top border, Padding="24,8":
        - `DockPanel`: Excluir button docked left (destructive style), Cancelar + Salvar buttons docked right (Salvar = accent style)
    - All interactive elements have `AutomationProperties.Name` and `AutomationProperties.HelpText`
    - ScrollViewer Padding="32,24,32,16" (2xl horizontal, xl top, lg bottom — per UI-SPEC spacing)

    **7. Create `ConteudoDetalheWindow.axaml.cs`**:
    - Override `OnClosing(WindowClosingEventArgs e)`: if ViewModel.IsDirty, cancel close, show discard confirmation dialog. If user confirms discard, close with `false`. Pattern from research lines 741-752.
    - Override `OnOpened`: trigger ViewModel.CarregarAsync()

    **8. Create `ConteudoDetalheViewModel.cs`** — `partial class` using CommunityToolkit.Mvvm:
    - Properties for all fields in Identificação section: Titulo, Descricao, Anotacoes, Formato (as FormatoMidia enum), Subtipo
    - Properties for display-only summary data: Classificacao, Nota, CategoriaCount, RelacaoCount, SessoesContagem, TotalEsperadoSessoes, IsFilho
    - **Dirty tracking**: snapshot all original values on load. `IsDirty` computed property compares current vs. original for each field. `TituloJanela` appends " •" when dirty.
    - `[RelayCommand] CarregarAsync()`: send `ObterConteudoQuery`, populate all fields, snapshot originals
    - `[RelayCommand] SalvarAsync()`: validate titulo non-empty, build `AtualizarConteudoCommand` from current fields, send via MediatR, if success → close window with `true`, if failure → show error
    - `[RelayCommand] ExcluirAsync()`: show confirmation dialog ("Excluir conteúdo" per UI-SPEC), if confirmed → send `ExcluirConteudoCommand`, close window with `null`
    - `[RelayCommand] CancelarCommand`: if dirty → show discard confirmation, if confirmed or not dirty → close window with `false`
    - Inject: `IMediator`, `IDialogService`, `Guid conteudoId` (via factory)
    - Expander summary properties (computed, read-only):
      - `ResumoAvaliacao`: "Nota: {Nota} · 👍" or "Sem avaliação"
      - `ResumoOrganizacao`: "{n} categorias · {m} relações" or "Sem categorias ou relações"
      - `ResumoHistorico`: "{n} de {total} ({%}%)" or "{n} sessões registradas" or "Sem sessões"

    **9. Register DI** in `App.axaml.cs`:
    - Register `IDialogService` → `DialogService` as singleton
    - Register `Func<Guid, ConteudoDetalheViewModel>` factory
    - Update `AcervoViewModel` constructor to accept `IDialogService`

    NOTE: Plans 05-06 will wire the Avaliação, Organização, and Histórico section contents. This plan creates the structural shell + Identificação section + dirty tracking + save/cancel/delete flow.
  </action>
  <verify>
    <automated>cd /media/belo/BeloSSD_2/Workspace/DiarioDeBordo && dotnet build src/DiarioDeBordo.UI --no-restore -v q 2>&1 | tail -5 && dotnet build src/DiarioDeBordo.Desktop --no-restore -v q 2>&1 | tail -5</automated>
  </verify>
  <done>ConteudoDetalheWindow compiles with 4 Expander sections. ConfirmacaoDialog compiles. IDialogService defined and implemented. ConteudoDetalheViewModel has dirty tracking, load, save, delete, cancel commands. DI registered in App.axaml.cs. All AutomationProperties set on interactive elements.</done>
</task>

<task type="auto">
  <name>Task 2: Enhanced ConteudoCard + AcervoViewModel updates</name>
  <files>
    src/DiarioDeBordo.UI/Views/AcervoView.axaml
    src/DiarioDeBordo.UI/ViewModels/AcervoViewModel.cs
  </files>
  <action>
    **1. Update ConteudoCard template in `AcervoView.axaml`** (per UI-SPEC section 2):
    Replace the current simple card DataTemplate with enhanced version:
    ```xml
    <Border Margin="0,0,0,8" Padding="16" BorderThickness="1"
            BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
            CornerRadius="4"
            AutomationProperties.Name="{Binding Titulo}">
        <Grid ColumnDefinitions="*,Auto">
            <!-- Left: content info -->
            <StackPanel Grid.Column="0" Spacing="4">
                <!-- Title row -->
                <Grid ColumnDefinitions="*,Auto">
                    <TextBlock Grid.Column="0" Text="{Binding Titulo}"
                               FontSize="14" VerticalAlignment="Center" />
                    <TextBlock Grid.Column="1" Text="{Binding Formato}"
                               FontSize="12" Opacity="0.6"
                               VerticalAlignment="Center" Margin="8,0,0,0" />
                </Grid>
                <!-- Metadata row (12px caption, Opacity=0.6) -->
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <!-- Category count -->
                    <TextBlock Text="{Binding CategoriaCount, StringFormat='{}{0} categorias'}"
                               FontSize="12" Opacity="0.6"
                               IsVisible="{Binding CategoriaCount}" />
                    <!-- Classificação icon -->
                    <TextBlock Text="👍" FontSize="12" Opacity="0.6"
                               IsVisible="{Binding IsGostei}" />
                    <TextBlock Text="👎" FontSize="12" Opacity="0.6"
                               IsVisible="{Binding IsNaoGostei}" />
                    <!-- Nota -->
                    <TextBlock Text="{Binding Nota, StringFormat='Nota {0:0.#}'}"
                               FontSize="12" Opacity="0.6"
                               IsVisible="{Binding Nota, Converter={x:Static ObjectConverters.IsNotNull}}" />
                </StackPanel>
            </StackPanel>
            <!-- Right: action buttons (always visible at Opacity=0.6 per UI-SPEC WCAG AAA) -->
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="4"
                        VerticalAlignment="Center">
                <Button Padding="8" Opacity="0.6"
                        Command="{Binding $parent[ItemsControl].((vm:AcervoViewModel)DataContext).AbrirDetalheCommand}"
                        CommandParameter="{Binding Id}"
                        AutomationProperties.Name="{Binding Titulo, StringFormat='Ver detalhe de {0}'}">
                    <TextBlock Text="👁" FontSize="14" />
                </Button>
                <Button Padding="8" Opacity="0.6"
                        Command="{Binding $parent[ItemsControl].((vm:AcervoViewModel)DataContext).ExcluirCommand}"
                        CommandParameter="{Binding Id}"
                        AutomationProperties.Name="{Binding Titulo, StringFormat='Excluir {0}'}">
                    <TextBlock Text="🗑" FontSize="14" />
                </Button>
            </StackPanel>
        </Grid>
    </Border>
    ```
    NOTE on binding: The card action buttons need to bind to AcervoViewModel commands, not the DTO. Use Avalonia's `$parent[ItemsControl].DataContext` syntax for ancestor binding. The command parameter is the content Id.

    Title font size changed from 15px to 14px (UI-SPEC typography normalization).

    For `IsGostei`/`IsNaoGostei` — these can be computed via a converter or add boolean properties to the DTO. Simplest: use a value converter `ClassificacaoToVisibilityConverter` or add `IsGostei` and `IsNaoGostei` computed properties on `ConteudoResumoDto`. Since DTOs are records, consider using a wrapper ViewModel or a converter. Prefer converter for purity.

    **2. Update `AcervoViewModel.cs`**:
    - Inject `IDialogService` (add to constructor alongside existing `IMediator` and `Func<CriarConteudoViewModel>`)
    - Add `[RelayCommand] AbrirDetalheAsync(Guid conteudoId)`:
      ```csharp
      var resultado = await _dialogService.MostrarConteudoDetalheAsync(conteudoId);
      if (resultado is true or null) // modified or deleted
          await CarregarAsync();
      ```
    - Add `[RelayCommand] ExcluirAsync(Guid conteudoId)`:
      ```csharp
      var confirmado = await _dialogService.MostrarConfirmacaoAsync(
          Strings.Dialog_ExcluirConteudo_Titulo,
          string.Format(Strings.Dialog_ExcluirConteudo_Mensagem, conteudo.Titulo), // need to find title
          Strings.Modal_BotaoExcluir,
          Strings.Label_Cancelar,
          isPrimarioDestructivo: true);
      if (confirmado)
      {
          var result = await _mediator.Send(new ExcluirConteudoCommand(conteudoId, _usuarioIdTemporario));
          if (result.IsSuccess) await CarregarAsync();
      }
      ```
      NOTE: To get the title for the confirmation message, find it in the Conteudos collection by Id.

    - Update `CarregarAsync`: paginated list now returns enriched `ConteudoResumoDto` with Classificacao and CategoriaCount — no changes needed to loading logic, the DTO mapping is handled by the handler.

    **3. Add pagination controls** to `AcervoView.axaml` if not already present. The list already uses `PaginatedList<ConteudoResumoDto>` but the view only shows items — add page navigation buttons (Previous/Next) in the status bar area:
    ```xml
    <!-- Status bar with pagination (ACE-09) -->
    <Grid Grid.Row="2" Margin="24,4" ColumnDefinitions="*,Auto">
        <TextBlock Grid.Column="0"
                   Text="{Binding StatusText}"
                   FontSize="12" Opacity="0.5" />
        <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
            <Button Content="← Anterior"
                    Command="{Binding PaginaAnteriorCommand}"
                    IsVisible="{Binding TemPaginaAnterior}"
                    FontSize="12" Padding="8,4" />
            <Button Content="Próxima →"
                    Command="{Binding ProximaPaginaCommand}"
                    IsVisible="{Binding TemProximaPagina}"
                    FontSize="12" Padding="8,4" />
        </StackPanel>
    </Grid>
    ```
    Add pagination state to AcervoViewModel: `PaginaAtual`, `TemPaginaAnterior`, `TemProximaPagina`, `PaginaAnteriorCommand`, `ProximaPaginaCommand`. Update `CarregarAsync` to use the current page.
  </action>
  <verify>
    <automated>cd /media/belo/BeloSSD_2/Workspace/DiarioDeBordo && dotnet build src/DiarioDeBordo.Desktop --no-restore -v q 2>&1 | tail -5</automated>
  </verify>
  <done>ConteudoCard shows metadata row (category count, classificação icon, nota). Card has Ver detalhe (👁) and Excluir (🗑) buttons always visible at Opacity=0.6. AcervoViewModel opens modal via IDialogService and handles delete from card. Pagination controls visible in status bar. Card title normalized to 14px. Full solution compiles with zero warnings.</done>
</task>

</tasks>

<verification>
1. `dotnet build src/DiarioDeBordo.Desktop --no-restore -v q` — zero warnings
2. `dotnet test --no-restore -v q` — full suite passes (no UI test regressions)
3. All AutomationProperties set per UI-SPEC accessibility contract
4. ConfirmacaoDialog usable for both discard and delete confirmations
</verification>

<success_criteria>
- ConteudoDetalheWindow opens as modal (D-01) via explicit button (D-04)
- 4 Expander sections: Identificação expanded, others collapsed (D-05)
- Identificação section: editable Titulo, Descrição, Anotações, Formato, Subtipo
- Fixed footer: Excluir left, Cancelar + Salvar right (D-02, D-03)
- IsDirty tracking: closing with changes prompts discard dialog (D-02)
- Delete shows confirmation dialog (D-03) — both from modal and card
- ConfirmacaoDialog is reusable with configurable title/message/buttons
- Card metadata row shows category count, classificação icon, nota
- Card buttons always visible at Opacity=0.6 (WCAG AAA — no hover-only disclosure)
- Pagination controls in AcervoView (ACE-09)
- Full solution compiles and all tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-acervo-b-sico/03-04-SUMMARY.md`
</output>
