---
phase: 04-curadoria-colet-neas-e-fontes
plan: 05
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified:
  - tests/Tests.Integration/CenarioApendiceATests.cs
autonomous: true
requirements:
  - SEG-04

must_haves:
  truths:
    - "Scenario 1 (thematic film lists with contextual annotations) works end-to-end"
    - "Scenario 2 (CDs with source fallback to Spotify/YouTube) works end-to-end"
    - "Scenario 3 (physical sketchbook registered with manual progress) works end-to-end"
    - "Scenario 4 (nested study plan with sub-collections including Cybersecurity) works end-to-end"
    - "Scenario 5 (Resident Evil franchise with nested Guiada collections) works end-to-end"
  artifacts:
    - path: "tests/Tests.Integration/CenarioApendiceATests.cs"
      provides: "Integration tests for Appendix A scenarios 1-5"
      contains: "class CenarioApendiceATests"
      min_tests: 5
  key_links:
    - from: "tests/Tests.Integration/CenarioApendiceATests.cs"
      to: "src/Modules/Module.Acervo/Commands/AdicionarItemNaColetaneaHandler.cs"
      via: "MediatR dispatch for adding items to collections"
      pattern: "AdicionarItemNaColetaneaCommand"
    - from: "tests/Tests.Integration/CenarioApendiceATests.cs"
      to: "src/DiarioDeBordo.Infrastructure/Repositorios/ColetaneaRepository.cs"
      via: "Real database operations via repository"
      pattern: "ColetaneaRepository"
---

<objective>
Implement integration tests for all 5 scenarios from Appendix A of the Domain Definition v3 (SEG-04 requirement). These scenarios validate that the domain model correctly handles real-world use cases: thematic film lists with contextual annotations, CDs with source fallback, physical sketchbook registration, nested study plans, and franchise tracking with nested collections.

Purpose: SEG-04 requires all Appendix A scenarios to be executable and tested end-to-end. These tests validate that Phase 4 features work together correctly against a real PostgreSQL database.
Output: Integration test file with 5 test methods (one per scenario), all passing.
</objective>

<execution_context>
@/media/belo/BeloSSD_2/Workspace/DiarioDeBordo/.claude/get-shit-done/workflows/execute-plan.md
@/media/belo/BeloSSD_2/Workspace/DiarioDeBordo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-curadoria-colet-neas-e-fontes/04-CONTEXT.md
@.planning/phases/04-curadoria-colet-neas-e-fontes/04-02-SUMMARY.md
@.planning/phases/04-curadoria-colet-neas-e-fontes/04-03-SUMMARY.md

<appendix_a_scenarios>
## Apêndice A — Validação por Cenários (from especificacoes/1  - definicao-de-dominio.md)

### Cenário 1: "Crio listas de filmes temáticas e anoto 'esse não achei dublado' e 'assistir em dia chuvoso'"

Caminho no modelo: O usuário cria uma coletânea do tipo Miscelânea ("Filmes de Terror para Noites Chuvosas"). Adiciona conteúdos do formato "vídeo" com subtipo "filme". As anotações "não achei dublado" e "assistir em dia chuvoso" são registradas como **anotações contextuais** na relação conteúdo-coletânea (não no conteúdo em si).

**Test validation:** Create Miscelânea collection, add 2 films, set contextual annotations on each, verify annotations are retrieved correctly and are independent of the content's global annotation.

### Cenário 2: "Comprei CDs físicos e crio playlists com músicas de diversos CDs, com fallback para Spotify ou YouTube"

Caminho no modelo: Cada música é um conteúdo com formato "áudio", subtipo "música". Fonte primária: arquivo local (caminho no sistema de arquivos). Fontes secundárias: URL do Spotify, URL do YouTube, com prioridades 2 e 3. A playlist é uma coletânea do tipo Miscelânea contendo as músicas.

**Test validation:** Create 3 audio contents with local file as priority 1, Spotify URL as priority 2, YouTube URL as priority 3. Create Miscelânea playlist, add all 3 songs. Verify source priorities are preserved and retrievable.

### Cenário 3: "Um caderno físico de desenhos que quero registrar no sistema"

Caminho no modelo: O usuário cria um conteúdo com formato "nenhum" (sem mídia digital). Sem fontes. Preenche título, anotações, categorias. Pode subir fotos das páginas como imagens do conteúdo. Pode registrar progresso manual ("desenhei até a página 30").

**Test validation:** Create content with FormatoMidia.Nenhum, no sources, with annotations. Add an image (cover photo). Verify content is retrievable with image.

### Cenário 4: "Plano de estudo sobre programação contendo artigos, vídeos, playlists e outras coletâneas como 'Cybersecurity'"

Caminho no modelo: O plano de estudo é uma coletânea do tipo Guiada. Contém itens com formatos variados (texto/artigo, vídeo, misto/post) e outras coletâneas (ex: coletânea Guiada "Fundamentos de Cybersecurity"). A ordenação indica a sequência recomendada.

**Test validation:** Create Guiada "Programming Study Plan". Create Guiada "Cybersecurity Fundamentals" with 3 articles. Add various content (article, video, post) to main plan. Add Cybersecurity collection as an item. Verify nested structure is preserved and retrievable without cycles.

### Cenário 5: "Acompanhar a franquia Resident Evil com jogos, filmes e livros"

Caminho no modelo: O usuário cria uma coletânea Guiada "Resident Evil" com ordenação habilitada. Dentro dela, cria coletâneas filhas — "RE — Jogos" (Guiada), "RE — Filmes" (Guiada), "RE — Livros" (Guiada). Cada jogo, filme ou livro é um conteúdo com formato respectivo.

**Test validation:** Create parent Guiada "Resident Evil". Create 3 child Guiadas (Jogos, Filmes, Livros). Add 2 items to each child. Add all 3 child collections to parent. Verify nested structure, item counts, and that cycle detection would block adding parent to any child.
</appendix_a_scenarios>

<interfaces>
<!-- Contracts used by integration tests -->

From src/Modules/Module.Acervo/Commands/:
```csharp
record CriarConteudoCommand(Guid UsuarioId, string Titulo, ..., PapelConteudo Papel, TipoColetanea? TipoColetanea, ...);
record AdicionarItemNaColetaneaCommand(Guid ColetaneaId, Guid ConteudoId, Guid UsuarioId);
record AtualizarAnotacaoContextualCommand(Guid ColetaneaId, Guid ConteudoId, Guid UsuarioId, string? AnotacaoContextual);
record AdicionarFonteCommand(Guid ConteudoId, Guid UsuarioId, TipoFonte Tipo, string Valor, string? Plataforma);
record AdicionarImagemCapaCommand(Guid ConteudoId, Guid UsuarioId, string Caminho, long TamanhoBytes);
```

From src/Modules/Module.Acervo/Queries/:
```csharp
record ListarItensColetaneaQuery(Guid ColetaneaId, Guid UsuarioId, PaginacaoParams Paginacao);
record ObterColetaneaDetalheQuery(Guid ColetaneaId, Guid UsuarioId);
record ObterConteudoQuery(Guid Id, Guid UsuarioId);
```

From tests/Tests.Integration/ (existing pattern):
```csharp
public class SomeIntegrationTests : IAsyncLifetime
{
    private readonly PostgreSqlContainer _container;
    private ServiceProvider _serviceProvider;
    private IMediator _mediator;
    
    public async Task InitializeAsync() { /* Start container, build DI, apply migrations */ }
    public async Task DisposeAsync() { /* Cleanup */ }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create integration test file with Wave 0 test stubs</name>
  <files>
    tests/Tests.Integration/CenarioApendiceATests.cs
  </files>
  <read_first>
    tests/Tests.Integration/Repositorios/ConteudoRepositoryTests.cs
    tests/Tests.Integration/IntegrationTestBase.cs
    src/Modules/Module.Acervo/Commands/CriarConteudoCommand.cs
    src/Modules/Module.Acervo/Commands/AdicionarItemNaColetaneaCommand.cs
    src/Modules/Module.Acervo/Commands/AtualizarAnotacaoContextualCommand.cs
    src/Modules/Module.Acervo/Commands/AdicionarFonteCommand.cs
    src/Modules/Module.Acervo/Queries/ListarItensColetaneaQuery.cs
  </read_first>
  <behavior>
    - Cenario1: Create Miscelânea, add films, set contextual annotations, verify retrieval
    - Cenario2: Create songs with 3 sources each (local, Spotify, YouTube), verify priorities
    - Cenario3: Create content with FormatoMidia.Nenhum, no sources, add image
    - Cenario4: Create nested Guiadas (main + Cybersecurity), verify nested retrieval
    - Cenario5: Create parent Guiada with 3 child Guiadas, verify structure and cycle protection
  </behavior>
  <action>
    Create `tests/Tests.Integration/CenarioApendiceATests.cs` following the existing integration test pattern.

    **Test class structure:**
    ```csharp
    using DiarioDeBordo.Core.Enums;
    using DiarioDeBordo.Core.Paginacao;
    using MediatR;
    using Module.Acervo.Commands;
    using Module.Acervo.Queries;
    using Testcontainers.PostgreSql;
    using Xunit;

    namespace Tests.Integration;

    [Collection("IntegrationTests")]
    public class CenarioApendiceATests : IAsyncLifetime
    {
        private readonly PostgreSqlContainer _container = new PostgreSqlBuilder()
            .WithImage("postgres:16-alpine")
            .Build();
        
        private ServiceProvider? _serviceProvider;
        private IMediator _mediator = null!;
        private readonly Guid _usuarioId = Guid.NewGuid();

        public async Task InitializeAsync()
        {
            await _container.StartAsync();
            // Build service provider with real DbContext pointing to container
            // Apply migrations
            // Get IMediator from DI
        }

        public async Task DisposeAsync()
        {
            if (_serviceProvider != null)
                await _serviceProvider.DisposeAsync();
            await _container.DisposeAsync();
        }

        [Fact]
        public async Task Cenario1_ListasFilmesTematicasComAnotacoesContextuais()
        {
            // Arrange: Create Miscelânea "Filmes de Terror para Noites Chuvosas"
            var coletaneaResult = await _mediator.Send(new CriarConteudoCommand(
                _usuarioId, "Filmes de Terror para Noites Chuvosas",
                Papel: PapelConteudo.Coletanea, TipoColetanea: TipoColetanea.Miscelanea));
            Assert.True(coletaneaResult.Sucesso);
            var coletaneaId = coletaneaResult.Valor;

            // Add 2 films
            var filme1Result = await _mediator.Send(new CriarConteudoCommand(
                _usuarioId, "O Iluminado", Formato: FormatoMidia.Video));
            var filme2Result = await _mediator.Send(new CriarConteudoCommand(
                _usuarioId, "O Exorcista", Formato: FormatoMidia.Video));
            
            await _mediator.Send(new AdicionarItemNaColetaneaCommand(coletaneaId, filme1Result.Valor, _usuarioId));
            await _mediator.Send(new AdicionarItemNaColetaneaCommand(coletaneaId, filme2Result.Valor, _usuarioId));

            // Set contextual annotations (specific to this collection)
            await _mediator.Send(new AtualizarAnotacaoContextualCommand(
                coletaneaId, filme1Result.Valor, _usuarioId, "não achei dublado"));
            await _mediator.Send(new AtualizarAnotacaoContextualCommand(
                coletaneaId, filme2Result.Valor, _usuarioId, "assistir em dia chuvoso"));

            // Act: Retrieve items with annotations
            var itensResult = await _mediator.Send(new ListarItensColetaneaQuery(
                coletaneaId, _usuarioId, new PaginacaoParams(1, 10)));

            // Assert
            Assert.True(itensResult.Sucesso);
            Assert.Equal(2, itensResult.Valor.TotalItens);
            var item1 = itensResult.Valor.Itens.First(i => i.Titulo == "O Iluminado");
            var item2 = itensResult.Valor.Itens.First(i => i.Titulo == "O Exorcista");
            Assert.Equal("não achei dublado", item1.AnotacaoContextual);
            Assert.Equal("assistir em dia chuvoso", item2.AnotacaoContextual);
        }

        [Fact]
        public async Task Cenario2_CDsComFallbackParaSpotifyOuYouTube()
        {
            // Arrange: Create 3 songs with sources at different priorities
            var musica1Result = await _mediator.Send(new CriarConteudoCommand(
                _usuarioId, "Bohemian Rhapsody", Formato: FormatoMidia.Audio));
            Assert.True(musica1Result.Sucesso);
            var musica1Id = musica1Result.Valor;

            // Add sources with priorities: local=1, Spotify=2, YouTube=3
            await _mediator.Send(new AdicionarFonteCommand(musica1Id, _usuarioId, 
                TipoFonte.ArquivoLocal, "/mnt/cd1/track01.flac"));
            await _mediator.Send(new AdicionarFonteCommand(musica1Id, _usuarioId,
                TipoFonte.Url, "https://open.spotify.com/track/xyz", "Spotify"));
            await _mediator.Send(new AdicionarFonteCommand(musica1Id, _usuarioId,
                TipoFonte.Url, "https://youtube.com/watch?v=abc", "YouTube"));

            // Create playlist
            var playlistResult = await _mediator.Send(new CriarConteudoCommand(
                _usuarioId, "Minha Playlist de CDs",
                Papel: PapelConteudo.Coletanea, TipoColetanea: TipoColetanea.Miscelanea));
            await _mediator.Send(new AdicionarItemNaColetaneaCommand(
                playlistResult.Valor, musica1Id, _usuarioId));

            // Act: Get content detail with sources
            var conteudoResult = await _mediator.Send(new ObterConteudoQuery(musica1Id, _usuarioId));

            // Assert: Sources ordered by priority
            Assert.True(conteudoResult.Sucesso);
            Assert.Equal(3, conteudoResult.Valor.Fontes.Count);
            Assert.Equal(1, conteudoResult.Valor.Fontes[0].Prioridade);
            Assert.Equal("ArquivoLocal", conteudoResult.Valor.Fontes[0].Tipo);
            Assert.Equal(2, conteudoResult.Valor.Fontes[1].Prioridade);
            Assert.Contains("Spotify", conteudoResult.Valor.Fontes[1].Plataforma);
            Assert.Equal(3, conteudoResult.Valor.Fontes[2].Prioridade);
        }

        [Fact]
        public async Task Cenario3_CadernoFisicoDeDesenhos()
        {
            // Arrange: Create content with no digital media, no sources
            var cadernoResult = await _mediator.Send(new CriarConteudoCommand(
                _usuarioId, "Caderno de Desenhos 2024",
                Descricao: "Caderno Moleskine A5",
                Anotacoes: "Desenhei até a página 30",
                Formato: FormatoMidia.Nenhum));
            Assert.True(cadernoResult.Sucesso);
            var cadernoId = cadernoResult.Valor;

            // Add cover image (photo of the notebook)
            await _mediator.Send(new AdicionarImagemCapaCommand(
                cadernoId, _usuarioId, "/photos/caderno_capa.jpg", 500_000));

            // Act: Retrieve
            var conteudoResult = await _mediator.Send(new ObterConteudoQuery(cadernoId, _usuarioId));

            // Assert
            Assert.True(conteudoResult.Sucesso);
            Assert.Equal("Nenhum", conteudoResult.Valor.Formato);
            Assert.Empty(conteudoResult.Valor.Fontes);
            Assert.Single(conteudoResult.Valor.Imagens);
            Assert.Contains("Desenhei até a página 30", conteudoResult.Valor.Anotacoes);
        }

        [Fact]
        public async Task Cenario4_PlanoDeEstudoComColetaneasAninhadas()
        {
            // Arrange: Create Guiada "Cybersecurity Fundamentals" with articles
            var cyberResult = await _mediator.Send(new CriarConteudoCommand(
                _usuarioId, "Fundamentos de Cybersecurity",
                Papel: PapelConteudo.Coletanea, TipoColetanea: TipoColetanea.Guiada));
            var cyberId = cyberResult.Valor;

            var artigo1 = await _mediator.Send(new CriarConteudoCommand(
                _usuarioId, "OWASP Top 10", Formato: FormatoMidia.Texto));
            var artigo2 = await _mediator.Send(new CriarConteudoCommand(
                _usuarioId, "SQL Injection Prevention", Formato: FormatoMidia.Texto));
            await _mediator.Send(new AdicionarItemNaColetaneaCommand(cyberId, artigo1.Valor, _usuarioId));
            await _mediator.Send(new AdicionarItemNaColetaneaCommand(cyberId, artigo2.Valor, _usuarioId));

            // Create main study plan
            var planoResult = await _mediator.Send(new CriarConteudoCommand(
                _usuarioId, "Plano de Estudo: Programação",
                Papel: PapelConteudo.Coletanea, TipoColetanea: TipoColetanea.Guiada));
            var planoId = planoResult.Valor;

            // Add various content types
            var video = await _mediator.Send(new CriarConteudoCommand(
                _usuarioId, "Clean Code Video", Formato: FormatoMidia.Video));
            await _mediator.Send(new AdicionarItemNaColetaneaCommand(planoId, video.Valor, _usuarioId));

            // Add Cybersecurity collection as nested item
            await _mediator.Send(new AdicionarItemNaColetaneaCommand(planoId, cyberId, _usuarioId));

            // Act: Get plan detail
            var detalheResult = await _mediator.Send(new ObterColetaneaDetalheQuery(planoId, _usuarioId));
            var itensResult = await _mediator.Send(new ListarItensColetaneaQuery(
                planoId, _usuarioId, new PaginacaoParams(1, 10)));

            // Assert
            Assert.True(detalheResult.Sucesso);
            Assert.Equal("Guiada", detalheResult.Valor.TipoColetanea);
            Assert.Equal(2, itensResult.Valor.TotalItens); // video + cybersecurity collection
            
            var nestedCollection = itensResult.Valor.Itens.First(i => i.Titulo == "Fundamentos de Cybersecurity");
            Assert.Equal("Coletanea", nestedCollection.Papel);
            Assert.Equal(2, nestedCollection.SubItensContagem);
        }

        [Fact]
        public async Task Cenario5_FranquiaResidentEvilComColetaneasAninhadas()
        {
            // Arrange: Create parent Guiada
            var reResult = await _mediator.Send(new CriarConteudoCommand(
                _usuarioId, "Resident Evil",
                Papel: PapelConteudo.Coletanea, TipoColetanea: TipoColetanea.Guiada));
            var reId = reResult.Valor;

            // Create child Guiadas
            var jogosResult = await _mediator.Send(new CriarConteudoCommand(
                _usuarioId, "RE — Jogos",
                Papel: PapelConteudo.Coletanea, TipoColetanea: TipoColetanea.Guiada));
            var filmesResult = await _mediator.Send(new CriarConteudoCommand(
                _usuarioId, "RE — Filmes",
                Papel: PapelConteudo.Coletanea, TipoColetanea: TipoColetanea.Guiada));
            var livrosResult = await _mediator.Send(new CriarConteudoCommand(
                _usuarioId, "RE — Livros",
                Papel: PapelConteudo.Coletanea, TipoColetanea: TipoColetanea.Guiada));

            // Add items to each child
            var jogo1 = await _mediator.Send(new CriarConteudoCommand(
                _usuarioId, "Resident Evil 1", Formato: FormatoMidia.Interativo));
            var jogo2 = await _mediator.Send(new CriarConteudoCommand(
                _usuarioId, "Resident Evil 2", Formato: FormatoMidia.Interativo));
            await _mediator.Send(new AdicionarItemNaColetaneaCommand(jogosResult.Valor, jogo1.Valor, _usuarioId));
            await _mediator.Send(new AdicionarItemNaColetaneaCommand(jogosResult.Valor, jogo2.Valor, _usuarioId));

            var filme1 = await _mediator.Send(new CriarConteudoCommand(
                _usuarioId, "Resident Evil (2002)", Formato: FormatoMidia.Video));
            await _mediator.Send(new AdicionarItemNaColetaneaCommand(filmesResult.Valor, filme1.Valor, _usuarioId));

            var livro1 = await _mediator.Send(new CriarConteudoCommand(
                _usuarioId, "Resident Evil: The Umbrella Conspiracy", Formato: FormatoMidia.Texto));
            await _mediator.Send(new AdicionarItemNaColetaneaCommand(livrosResult.Valor, livro1.Valor, _usuarioId));

            // Add child collections to parent
            await _mediator.Send(new AdicionarItemNaColetaneaCommand(reId, jogosResult.Valor, _usuarioId));
            await _mediator.Send(new AdicionarItemNaColetaneaCommand(reId, filmesResult.Valor, _usuarioId));
            await _mediator.Send(new AdicionarItemNaColetaneaCommand(reId, livrosResult.Valor, _usuarioId));

            // Act: Get structure
            var detalheResult = await _mediator.Send(new ObterColetaneaDetalheQuery(reId, _usuarioId));
            var itensResult = await _mediator.Send(new ListarItensColetaneaQuery(
                reId, _usuarioId, new PaginacaoParams(1, 10)));

            // Assert structure
            Assert.True(detalheResult.Sucesso);
            Assert.Equal(3, detalheResult.Valor.QuantidadeItens);
            Assert.Equal(3, itensResult.Valor.TotalItens);

            // Verify cycle detection would block adding parent to any child
            var cycleResult = await _mediator.Send(new AdicionarItemNaColetaneaCommand(
                jogosResult.Valor, reId, _usuarioId));
            Assert.False(cycleResult.Sucesso);
            Assert.Contains("Ciclo", cycleResult.Erro!.Mensagem);
        }
    }
    ```

    **Implementation notes:**
    1. Follow existing integration test patterns from `tests/Tests.Integration/Repositorios/`
    2. Use `PostgreSqlContainer` from Testcontainers for real database
    3. Build full service provider with all DI registrations
    4. Apply EF Core migrations on container startup
    5. Each test validates a complete user scenario end-to-end
    6. Scenario 5 explicitly tests cycle detection (adding parent to child fails)
  </action>
  <verify>
    <automated>dotnet test tests/Tests.Integration/ --filter "FullyQualifiedName~CenarioApendiceA" -x</automated>
  </verify>
  <acceptance_criteria>
    - `tests/Tests.Integration/CenarioApendiceATests.cs` exists
    - File contains `class CenarioApendiceATests`
    - File contains exactly 5 `[Fact]` methods:
      - `Cenario1_ListasFilmesTematicasComAnotacoesContextuais`
      - `Cenario2_CDsComFallbackParaSpotifyOuYouTube`
      - `Cenario3_CadernoFisicoDeDesenhos`
      - `Cenario4_PlanoDeEstudoComColetaneasAninhadas`
      - `Cenario5_FranquiaResidentEvilComColetaneasAninhadas`
    - Each test validates the scenario described in Appendix A
    - Cenario5 explicitly tests cycle detection failure
    - `dotnet test tests/Tests.Integration/ --filter "FullyQualifiedName~CenarioApendiceA" -x` exits 0
    - All 5 tests pass
  </acceptance_criteria>
  <done>All 5 Appendix A scenarios implemented as integration tests. SEG-04 requirement fulfilled.</done>
</task>

</tasks>

<verification>
- `dotnet test tests/Tests.Integration/ --filter "FullyQualifiedName~CenarioApendiceA" -x` passes
- All 5 scenarios from Appendix A are covered
- Tests use real PostgreSQL via Testcontainers
- Cycle detection is explicitly tested in Scenario 5
- Each test validates complete user journey end-to-end
</verification>

<success_criteria>
- 5 integration tests (one per Appendix A scenario)
- All tests pass against real PostgreSQL
- SEG-04 requirement fully satisfied
- Tests validate commands, queries, and domain invariants working together
</success_criteria>

<output>
After completion, create `.planning/phases/04-curadoria-colet-neas-e-fontes/04-05-SUMMARY.md`
</output>
