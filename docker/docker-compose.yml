services:
  frontend:
    build:
      context: ..
      dockerfile: frontend/Dockerfile
    ports:
      - "4200:80"
    # A URL da API é configurada em tela pelo usuário admin na aplicação (menu Configurações).
    # Não é necessária variável de ambiente no container para a URL da API.
